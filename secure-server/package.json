{
  "name": "dhanutech-secure-server",
  "version": "1.0.0",
  "private": true,
  "description": "Security-hardened backend for Dhanu Tech static site",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test tests/security.test.js",
    "gen:cert": "openssl req -x509 -newkey rsa:2048 -nodes -keyout certs/dev-key.pem -out certs/dev-cert.pem -days 365 -subj '/CN=localhost'"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "express": "^4.21.2",
    "express-rate-limit": "^7.4.1",
    "express-session": "^1.18.1",
    "express-validator": "^7.2.1",
    "helmet": "^8.0.0",
    "sqlite3": "^5.1.7",
    "supertest": "^7.0.0"
  }
}